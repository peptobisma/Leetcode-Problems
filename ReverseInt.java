class Solution {
    public int reverse(int x) {
            char[] numToCharArray = ("" + x).toCharArray();
            int end = numToCharArray.length - 1;
            while (0 <= numToCharArray.length) {
                    print
            }
    }

        }